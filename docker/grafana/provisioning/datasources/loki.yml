apiVersion: 1

datasources:
  - name: Loki
    uid: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    jsonData:
      derivedFields:
        - name: trace_id
          matcherRegex: "\"trace_id\":\"([0-9a-f]{32})\""
          url: "$${__value.raw}"
          datasourceUid: Tempo

